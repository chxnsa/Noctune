<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <!-- Toolbar Utama Berkarakter Poster Musik Modern (Fix Clean Structure) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/bg_surface"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp">

        <!-- Kiri: Menu Hamburger (≡) -->
        <LinearLayout
            android:id="@+id/tv_menu"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:orientation="vertical"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true">

            <View
                android:layout_width="22dp"
                android:layout_height="3dp"
                android:background="@color/text_primary"/>

            <View
                android:layout_width="22dp"
                android:layout_height="3dp"
                android:background="@color/text_primary"
                android:layout_marginTop="5dp"/>

            <View
                android:layout_width="22dp"
                android:layout_height="3dp"
                android:background="@color/text_primary"
                android:layout_marginTop="5dp"/>
        </LinearLayout>

        <!-- Tengah: Judul Aplikasi (Mengisi sisa ruang kosong agar tidak menabrak) -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:text="NOCTUNE"
            android:textColor="@color/text_primary"
            android:textSize="28sp"
            android:fontFamily="@font/bebas_neue"
            android:letterSpacing="0.15"/>

        <!-- Kanan: Group Aksi (Toggle Theme & Search) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- TOGGLE TEMA — Square Switch LIGHT/DARK -->
            <LinearLayout
                android:id="@+id/theme_toggle_container"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:orientation="horizontal"
                android:background="@color/text_primary"
                android:layout_marginEnd="12dp">

                <!-- LIGHT -->
                <TextView
                    android:id="@+id/tv_mode_light"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="LIGHT"
                    android:textSize="10sp"
                    android:fontFamily="@font/space_mono"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:paddingHorizontal="10dp"
                    android:clickable="true"
                    android:focusable="true"/>

                <!-- DARK -->
                <TextView
                    android:id="@+id/tv_mode_dark"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="DARK"
                    android:textSize="10sp"
                    android:fontFamily="@font/space_mono"
                    android:textStyle="bold"
                    android:gravity="center"
                    android:paddingHorizontal="10dp"
                    android:clickable="true"
                    android:focusable="true"/>

            </LinearLayout>

            <!-- TOMBOL SEARCH -->
            <TextView
                android:id="@+id/tv_search_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SRCH"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:fontFamily="@font/space_mono"
                android:textStyle="bold"
                android:clickable="true"
                android:focusable="true"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Garis Batas Atas -->
    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@color/border_color"/>

    <!-- Wadah Konten Fragment Utama -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Garis Batas Bawah -->
    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@color/border_color"/>

    <!-- Navigasi Menu Bawah -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/bg_surface"
        app:elevation="0dp"
        app:itemRippleColor="@null"
        app:itemTextColor="@drawable/bottom_nav_selector"
        app:itemIconTint="@drawable/bottom_nav_selector"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"/>

</LinearLayout>