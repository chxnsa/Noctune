<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <!-- Toolbar Utama Berkarakter Poster Musik Modern -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/bg_surface"
        android:paddingHorizontal="16dp">

        <!-- Pengganti ≡ menggunakan layout garis ikon khas Swiss style -->
        <LinearLayout
            android:id="@+id/tv_menu"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:orientation="vertical"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true">
            <View android:layout_width="22dp" android:layout_height="3dp" android:background="@color/text_primary"/>
            <View android:layout_width="22dp" android:layout_height="3dp" android:background="@color/text_primary" android:layout_marginTop="5dp"/>
            <View android:layout_width="22dp" android:layout_height="3dp" android:background="@color/text_primary" android:layout_marginTop="5dp"/>
        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="NOCTUNE"
            android:textColor="@color/text_primary"
            android:textSize="28sp"
            android:fontFamily="@font/bebas_neue"
            android:letterSpacing="0.15"/>

        <!-- Pengganti emoji 🔍 dengan layout text bergaya minimalis -->
        <TextView
            android:id="@+id/tv_search_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:text="SRCH"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:fontFamily="@font/space_mono"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true"/>
    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@color/border_color"/>

    <!-- Kontainer Fragment -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@color/border_color"/>

    <!-- Bottom Navigation Kotak Brutal Modern -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/bg_surface"
        app:elevation="0dp"
        app:itemRippleColor="@null"
        app:itemTextColor="@color/text_primary"
        app:itemIconTint="@color/text_primary"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"/>

</LinearLayout>